{
  "name": "xbo-workspace",
  "version": "1.0.1",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "client": "pnpm start --filter ./packages/client",
    "server": "pnpm start --filter ./packages/server",
    "server:dev": "pnpm start:dev --filter ./packages/server"
  }
}
